
var each = require('..');
var myarray = [{id: 1}, {id: 2}, {id: 3}];

each( myarray, function(element, index, next){
  console.log('element: ', element, '@', index);
  setTimeout(next, 500);
}), function(err){
  console.log(err ? err.message : 'Done');
});
